# Copy to .env and fill in your free Gemini API key (https://aistudio.google.com/apikey).
# Without a key the pipeline still runs, using the deterministic fallback extractor.
GEMINI_API_KEY=
# Optional: override the model (default gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
